# การอธิบายการทำงาน JavaScript (Week 5)

เอกสารนี้สรุปแนวคิดและการทำงานของโค้ด JavaScript ในแต่ละไฟล์  
โดยอธิบายแบบสั้น เข้าใจง่าย และอ้างอิงจากผลลัพธ์ที่แสดงใน Console

---

## 2.1 ไฟล์ 01-variables.js

### 📘 Challenge: Create a Person Object

- สร้าง Object `student` เพื่อเก็บข้อมูลนักศึกษา
- ใช้ Method ภายใน Object เพื่อจัดการข้อมูลของตัวเอง
- ใช้ `this` อ้างอิงข้อมูลภายใน Object
- ใช้ `.join()` แปลง Array รายวิชาเป็นข้อความ

**ผลลัพธ์**
Full name: Alice Smith
Info: Alice Smith, Age: 20, GPA: 3.8
Courses: HTML, CSS, JavaScript

---

## 2.2 ไฟล์ 02-functions.js

### 📘 Returning Objects

- ฟังก์ชัน `createUser()` สร้าง Object และส่งค่ากลับ
- ใช้ Property และ Method shorthand
- สร้าง Email อัตโนมัติจากชื่อผู้ใช้

**ผลลัพธ์**
Email: john.doe@example.com
Full name: John Doe

### 📘 Callback Function

- ส่งฟังก์ชันเข้าไปเป็นพารามิเตอร์
- ใช้ Callback กำหนดรูปแบบการคำนวณข้อมูลใน Array

**ผลลัพธ์**
Doubled: [2, 4, 6, 8, 10]
Squared: [1, 4, 9, 16, 25]

---

## 2.3 ไฟล์ 03-control-flow.js

### 📘 Short-Circuit Evaluation

- ใช้ `?.` ป้องกัน Error
- ใช้ `||` กำหนดค่าเริ่มต้น
- ใช้ `&&` ตรวจสอบก่อนเข้าถึงข้อมูล

**ผลลัพธ์**
User name: John
User profile: undefined

### 📘 Form Validation

- ตรวจสอบข้อมูลจากฟอร์มตามเงื่อนไข
- เก็บ Error ทั้งหมดไว้ใน Array
- สรุปผลด้วย `isValid`

**ผลลัพธ์**
Valid user: { isValid: true }
Invalid user: { isValid: false }

---

## 2.4 ไฟล์ 04-loops.js

### 📘 Method Chaining

- ใช้ `filter → map → join`
- ใช้ `reduce` คำนวณค่าเฉลี่ย

**ผลลัพธ์**
Even numbers squared: 2²=4, 4²=16, ...
Average: 30

### 📘 Student Grades

- วิเคราะห์ข้อมูลนักเรียนด้วย Array Methods
- คำนวณค่าเฉลี่ยและหา Top Scorer

**ผลลัพธ์**
Top scorer: Alice (95)
Class average: 87.00

---

## 2.5 ไฟล์ 05-integration.js

### 📘 Quiz Application

- รวมทุกแนวคิดเข้าด้วยกัน
- จำลองการตอบคำถาม
- ตรวจคำตอบ คำนวณคะแนน และสถิติ

**ผลลัพธ์ตัวอย่าง**
FINAL SCORE: 3/5 (60.0%)
GRADE: D

---

## ✅ สรุป

Week 5 ช่วยให้เข้าใจพื้นฐาน JavaScript ตั้งแต่
Variables, Functions, Control Flow, Loops
จนถึงการนำไปใช้งานจริงในโปรแกรมเดียว
